<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login to nxtpick</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Space+Grotesk:wght@600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="login.css">
</head>
<style>
    :root {
    /* Color palette consistent with main app */
    --deep-purple: #5B21B6;
    --vibrant-purple: #8B5CF6;
    --electric-blue: #3B82F6;
    --bright-cyan: #06B6D4;
    
    /* Background colors */
    --bg-primary: #0F172A;
    --bg-secondary: #1E293B;
    --bg-card: #334155;
    --bg-hover: #475569;
    
    /* Text colors */
    --text-primary: #F8FAFC;
    --text-secondary: #CBD5E1;
    --text-tertiary: #94A3B8;
    
    /* Accent colors */
    --accent: var(--vibrant-purple);
    --accent-hover: var(--bright-cyan);
    --success: #10B981;
    --danger: #EF4444;
    
    /* Gradients */
    --gradient: linear-gradient(135deg, var(--deep-purple) 0%, var(--vibrant-purple) 50%, var(--electric-blue) 100%);
    --accent-gradient: linear-gradient(135deg, var(--vibrant-purple) 0%, var(--bright-cyan) 100%);
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', sans-serif;
    background-color: var(--bg-primary);
    color: var(--text-primary);
    line-height: 1.6;
    overflow: hidden;
}

.login-container {
    display: flex;
    min-height: 100vh;
}

.login-form-container {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 2rem;
    background: var(--bg-primary);
    position: relative;
    overflow: hidden;
}

.login-form-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 10% 20%, rgba(91, 33, 182, 0.15) 0%, transparent 20%),
                radial-gradient(circle at 90% 80%, rgba(59, 130, 246, 0.1) 0%, transparent 20%);
    z-index: 0;
}

.logo-container {
    text-align: center;
    margin-bottom: 2rem;
    position: relative;
    z-index: 1;
}

.logo {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    margin-bottom: 0.5rem;
}

.logo-image {
    width: 80px;
    height: 80px;
    border-radius: 16px;
    background: white;
    padding: 10px;
    transition: transform 0.3s ease;
}

.logo-image:hover {
    transform: scale(1.05);
}

.logo-text {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 2rem;
    font-weight: 700;
    background: var(--gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.tagline {
    color: var(--text-secondary);
    font-size: 1rem;
}

.login-form {
    width: 100%;
    max-width: 420px;
    background: rgba(30, 41, 59, 0.5);
    backdrop-filter: blur(10px);
    border-radius: 24px;
    padding: 2.5rem;
    border: 1px solid rgba(139, 92, 246, 0.2);
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    position: relative;
    z-index: 1;
}

.login-form h2 {
    font-size: 1.875rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    background: var(--gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.subtitle {
    color: var(--text-secondary);
    margin-bottom: 2rem;
    font-size: 0.95rem;
}

.form-group {
    margin-bottom: 1.5rem;
}

.form-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 500;
    color: var(--text-secondary);
    font-size: 0.875rem;
}

.input-container {
    position: relative;
    display: flex;
    align-items: center;
}

.input-container i {
    position: absolute;
    left: 1rem;
    color: var(--text-tertiary);
}

.input-container input {
    width: 100%;
    padding: 0.875rem 1rem 0.875rem 3rem;
    background: var(--bg-secondary);
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 12px;
    color: var(--text-primary);
    font-size: 1rem;
    transition: all 0.3s ease;
}

.input-container input:focus {
    outline: none;
    border-color: var(--accent);
    box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
}

.toggle-password {
    position: absolute;
    right: 1rem;
    background: none;
    border: none;
    color: var(--text-tertiary);
    cursor: pointer;
    transition: color 0.3s ease;
}

.toggle-password:hover {
    color: var(--text-primary);
}

.form-options {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    font-size: 0.875rem;
}

.remember-me {
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.remember-me input {
    width: 1rem;
    height: 1rem;
    accent-color: var(--accent);
}

.forgot-password {
    color: var(--accent);
    text-decoration: none;
    transition: color 0.3s ease;
}

.forgot-password:hover {
    color: var(--accent-hover);
}

.login-button {
    width: 100%;
    padding: 0.875rem;
    background: var(--gradient);
    border: none;
    border-radius: 12px;
    color: white;
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-bottom: 1.5rem;
}

.login-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 15px -3px rgba(139, 92, 246, 0.3);
}

.divider {
    display: flex;
    align-items: center;
    margin-bottom: 1.5rem;
    color: var(--text-tertiary);
    font-size: 0.875rem;
}

.divider::before,
.divider::after {
    content: '';
    flex: 1;
    height: 1px;
    background: rgba(139, 92, 246, 0.2);
}

.divider span {
    padding: 0 1rem;
}

.social-login {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
}

.social-button {
    flex: 1;
    padding: 0.75rem;
    background: var(--bg-secondary);
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 12px;
    color: var(--text-primary);
    font-weight: 500;
    font-size: 0.875rem;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
}

.social-button:hover {
    background: var(--bg-hover);
    border-color: var(--accent);
}

.social-button.google:hover {
    color: #EA4335;
}

.social-button.facebook:hover {
    color: #1877F2;
}

.social-button.apple:hover {
    color: white;
}

.login-image-container {
    flex: 1;
    position: relative;
    background: url('https://images.unsplash.com/photo-1611162617213-7d7a39e9b1d7?ixlib=rb-4.0.3&auto=format&fit=crop&w=2000&q=80') center/cover no-repeat;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.image-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(91, 33, 182, 0.8) 0%, rgba(59, 130, 246, 0.8) 100%);
    z-index: 1;
}

.login-content {
    max-width: 500px;
    padding: 2rem;
    text-align: center;
    position: relative;
    z-index: 2;
}

.login-content h1 {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    color: white;
    line-height: 1.2;
}

.login-content p {
    font-size: 1.125rem;
    margin-bottom: 2.5rem;
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.6;
}

.features {
    display: flex;
    justify-content: center;
    gap: 2rem;
}

.feature {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
}

.feature i {
    font-size: 2rem;
    color: white;
    background: rgba(255, 255, 255, 0.2);
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
}

.feature i:hover {
    background: rgba(255, 255, 255, 0.3);
    transform: scale(1.1);
}

.feature span {
    color: white;
    font-weight: 500;
}

/* Responsive Design */
@media (max-width: 1024px) {
    .login-image-container {
        display: none;
    }
    
    .login-form-container {
        flex: 1;
    }
}

@media (max-width: 640px) {
    .login-form {
        padding: 2rem 1.5rem;
    }
    
    .login-form h2 {
        font-size: 1.5rem;
    }
    
    .social-login {
        flex-direction: column;
    }
    
    .features {
        flex-direction: column;
        gap: 1.5rem;
    }
}

/* Animation for form inputs */
@keyframes inputFocus {
    0% {
        box-shadow: 0 0 0 0 rgba(139, 92, 246, 0.1);
    }
    100% {
        box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
    }
}

.input-container input:focus {
    animation: inputFocus 0.3s ease forwards;
}
</style>
<body>
    <div class="login-container">
        <div class="login-form-container">
            <div class="logo-container">
                <div class="logo">
                    <i class="fas fa-compass"></i>
                    <span class="logo-text">nxtpick</span>
                </div>
                <p class="tagline">Discover Your Next Favorite</p>
            </div>
            
            <form class="login-form" id="loginForm" action="/login" method="POST">
                <h2>Login to Your Account</h2>
                <p class="subtitle">Enter your credentials to access your personalized recommendations</p>
                
                <div class="form-group">
                    <label for="email">Email Address</label>
                    <div class="input-container">
                        <i class="fas fa-envelope"></i>
                        <input type="email" id="email" name="email" placeholder="" required>
                    </div>
                </div>
                
                <div class="form-group">
                    <label for="password">Password</label>
                    <div class="input-container">
                        <i class="fas fa-lock"></i>
                        <input type="password" id="password" name="password" placeholder="••••••••" required>
                        <button type="button" class="toggle-password" id="togglePassword">
                            <i class="fas fa-eye"></i>
                        </button>
                    </div>
                </div>
                
                <div class="form-options">
                    <div class="remember-me">
                        <input type="checkbox" id="remember" name="remember">
                        <label for="remember">Remember me</label>
                    </div>
                    <a href="#" class="forgot-password">Forgot password?</a>
                </div>
                
                <button type="submit" class="login-button">Login</button>
                
                <div class="divider">
                    <span>or continue with</span>
                </div>
                
                <div class="social-login">
                    <button type="button" class="social-button google">
                        <i class="fab fa-google"></i>
                        Google
                    </button>
                    
                </div>
            </form>
        </div>
        
        <div class="login-image-container">
            <div class="image-overlay"></div>
            <div class="login-content">
                <h1>Welcome Back to nxtpick</h1>
                <p>Your personalized recommendations are waiting. Login to continue your journey of discovering amazing movies, books, and music tailored just for you.</p>
                <div class="features">
                    <div class="feature">
                        <i class="fas fa-brain"></i>
                        <span>Smart AI</span>
                    </div>
                    <div class="feature">
                        <i class="fas fa-heart"></i>
                        <span>Personalized</span>
                    </div>
                    <div class="feature">
                        <i class="fas fa-bolt"></i>
                        <span>Instant</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script src="login.js"></script>
</body>
</html>