<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Forgot Password</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Orbitron:wght@700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
  <div class="auth-card">
    <h1 class="auth-title">Reset Access</h1>
    <p class="auth-subtitle">Enter your email to receive a secure reset link.</p>
    <form id="forgotPasswordForm" method="POST" action="/forgot-password" novalidate>
      <div class="auth-field">
        <label for="resetEmail">Email</label>
        <input id="resetEmail" name="email" type="email" placeholder="you@example.com" required />
      </div>
      <div class="auth-actions">
        <button type="submit" class="auth-btn">Send Reset Link</button>
      </div>
      <p class="alt"><a href="/login">Back to Sign in</a></p>
    </form>
  </div>
  <% if (message) { %>
    <div id="authBanner" class="auth-banner success" style="display:block" aria-live="polite"><%= message %></div>
  <% } else { %>
    <div id="authBanner" class="auth-banner" style="display:none" aria-live="polite"></div>
  <% } %>

  <script src="/js/show-banner.js"></script>
</body>
</html>