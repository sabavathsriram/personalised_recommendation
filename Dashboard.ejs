<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f5f6fa; text-align: center; padding-top: 50px; }
    .container { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 6px 16px rgba(0,0,0,0.1); width: 400px; margin: auto; }
    h2 { color: #333; }
    p { font-size: 16px; }
  </style>
</head>
<body>
  <div class="container">
    <h2>Welcome, <%= user.username %> 🎉</h2>
    <p>Your email: <%= user.email %></p>
    <p>Account created on: <%= user.createdAt.toDateString() %></p>
  </div>
</body>
</html>
